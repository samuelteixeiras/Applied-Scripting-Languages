from string import ascii_lowercase

ascii_lowercase.isalnum
count = 0
variable_name = "abc"
for character in variable_name:
    index = ascii_lowercase.find(character)
    print(ascii_lowercase[25- index])

print("Encripted message")